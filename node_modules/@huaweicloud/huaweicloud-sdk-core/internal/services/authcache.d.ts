export declare class AuthCache {
    private authMap;
    constructor();
    static instance(): AuthCache;
    putCache(key: string, value: string): void;
    getCache(key: string): string | undefined;
}
