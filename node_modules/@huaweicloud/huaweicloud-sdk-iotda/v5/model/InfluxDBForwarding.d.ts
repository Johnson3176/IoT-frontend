import { ColumnMapping } from './ColumnMapping';
import { NetAddress } from './NetAddress';
export declare class InfluxDBForwarding {
    address: NetAddress;
    private 'db_name';
    username: string;
    password: string;
    measurement: string;
    private 'column_mappings';
    constructor(address?: any, dbName?: any, username?: any, password?: any, measurement?: any, columnMappings?: any);
    withAddress(address: NetAddress): InfluxDBForwarding;
    withDbName(dbName: string): InfluxDBForwarding;
    set dbName(dbName: string | undefined);
    get dbName(): string | undefined;
    withUsername(username: string): InfluxDBForwarding;
    withPassword(password: string): InfluxDBForwarding;
    withMeasurement(measurement: string): InfluxDBForwarding;
    withColumnMappings(columnMappings: Array<ColumnMapping>): InfluxDBForwarding;
    set columnMappings(columnMappings: Array<ColumnMapping> | undefined);
    get columnMappings(): Array<ColumnMapping> | undefined;
}
