export declare class NetAddress {
    ip?: string;
    port?: number;
    domain?: string;
    constructor();
    withIp(ip: string): NetAddress;
    withPort(port: number): NetAddress;
    withDomain(domain: string): NetAddress;
}
