import { RuleCondition } from './RuleCondition';
import { TimeRange } from './TimeRange';
export declare class ConditionGroup {
    conditions?: Array<RuleCondition>;
    logic?: string;
    private 'time_range'?;
    constructor();
    withConditions(conditions: Array<RuleCondition>): ConditionGroup;
    withLogic(logic: string): ConditionGroup;
    withTimeRange(timeRange: TimeRange): ConditionGroup;
    set timeRange(timeRange: TimeRange | undefined);
    get timeRange(): TimeRange | undefined;
}
