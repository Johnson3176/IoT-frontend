import { CreatePortOption } from './CreatePortOption';
export declare class CreatePortRequestBody {
    port: CreatePortOption;
    constructor(port?: any);
    withPort(port: CreatePortOption): CreatePortRequestBody;
}
