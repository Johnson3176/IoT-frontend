export declare class ListTag {
    key: string;
    values: Array<string>;
    constructor(key?: any, values?: any);
    withKey(key: string): ListTag;
    withValues(values: Array<string>): ListTag;
}
