export declare class PageInfo {
    private 'previous_marker';
    private 'current_count';
    private 'next_marker'?;
    constructor(previousMarker?: any, currentCount?: any);
    withPreviousMarker(previousMarker: string): PageInfo;
    set previousMarker(previousMarker: string | undefined);
    get previousMarker(): string | undefined;
    withCurrentCount(currentCount: number): PageInfo;
    set currentCount(currentCount: number | undefined);
    get currentCount(): number | undefined;
    withNextMarker(nextMarker: string): PageInfo;
    set nextMarker(nextMarker: string | undefined);
    get nextMarker(): string | undefined;
}
