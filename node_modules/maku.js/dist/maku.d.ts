import * as THREE from "three";
import { HTMLIVCElement, MakuConfig, Scroll, Segments } from "../types/types";
declare class Maku {
    el: HTMLIVCElement;
    rect: DOMRect;
    mesh: THREE.Mesh | THREE.Points;
    scene: THREE.Scene;
    segments: Segments;
    constructor(el: HTMLIVCElement, material: THREE.ShaderMaterial, scene: THREE.Scene, config?: MakuConfig);
    setPosition(deltaY?: number): void;
    destroy(): void;
}
declare class MakuGroup {
    makus: Maku[];
    constructor();
    add(maku: Maku): Maku;
    addMultiple(makus: Maku[]): void;
    setPositions(deltaY?: number): void;
    clear(): void;
}
declare class Scroller {
    scroll: Scroll;
    constructor();
    listenForScroll(): void;
    syncScroll(): void;
}
declare const getScreenFov: (z: number) => number;
export { Maku, MakuGroup, Scroller, getScreenFov };
