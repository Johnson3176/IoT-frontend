import { PrimitiveSDF, SDFConfig } from "./primitive";
export interface PolygonSDFConfig extends SDFConfig {
    radius: number;
    edgeCount: number;
    angleDivisor: number;
    depth: number;
}
declare class PolygonSDF extends PrimitiveSDF {
    radius: number;
    edgeCount: number;
    angleDivisor: number;
    depth: number;
    constructor(config?: Partial<PolygonSDFConfig>);
    get shader(): string;
}
export { PolygonSDF };
