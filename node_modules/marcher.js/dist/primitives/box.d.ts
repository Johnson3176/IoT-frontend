import { PrimitiveSDF, SDFConfig } from "./primitive";
export interface BoxSDFConfig extends SDFConfig {
    width: number;
    height: number;
    depth: number;
}
declare class BoxSDF extends PrimitiveSDF {
    width: number;
    height: number;
    depth: number;
    constructor(config?: Partial<BoxSDFConfig>);
    get shader(): string;
}
export { BoxSDF };
