import { PrimitiveSDF, SDFConfig } from "./primitive";
export interface SphereSDFConfig extends SDFConfig {
    radius: number;
}
declare class SphereSDF extends PrimitiveSDF {
    radius: number;
    constructor(config?: Partial<SphereSDFConfig>);
    get shader(): string;
}
export { SphereSDF };
