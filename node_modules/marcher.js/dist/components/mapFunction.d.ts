import { SDFLayer } from "../components/layer";
declare class SDFMapFunction {
    layers: SDFLayer[];
    constructor();
    addLayer(layer: SDFLayer): this;
    get layerShader(): string;
    get shader(): string;
}
export { SDFMapFunction };
